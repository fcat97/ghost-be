product:
  type: kmp/lib
  platforms: [ linuxX64, mingwX64, macosArm64 ]

dependencies:
  - $ktor.server.core
  - $ktor.server.cio
  - com.charleskorn.kaml:kaml:0.61.0

test-dependencies:
  - $ktor.server.testHost

settings:
  ktor: enabled
  kotlin:
    serialization: json
