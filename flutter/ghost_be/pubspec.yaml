name: ghost_be
description: A dio interceptor that mocks HTTP responses via ghost-be during development.
version: 0.1.0
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.4.0

dev_dependencies:
  test: ^1.25.0
